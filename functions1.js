"use strict";

function sayHello(firstName, lastName) {
  console.log("Hello " + firstName + " " + lastName);
}

const myFirstName = "Gustav";
const myLastName = "Pedersen";
sayHello(myFirstName, myLastName);
