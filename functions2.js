"use strict";

function presentPet(firstName, animalType, animalName) {
  console.log("Hello, my name is " + firstName + ", I have a " + animalType + " called " + animalName);
}

const myFirstName = "Gustav";
const myAnimalType = "dog"
const myAnimalName = "Haku"
presentPet(myFirstName, myAnimalType, myAnimalName);

